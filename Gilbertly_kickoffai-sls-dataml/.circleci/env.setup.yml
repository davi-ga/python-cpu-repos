default: &default
  PROFILE: default
  REGION: region
  DB_NAME: db_name

dev:
  <<: *default
  STAGE: dev_stage
  DEPLOY_BUCKET: dev_deploy_bucket
  ARTIFACT_BUCKET: dev_deploy_bucket
  TABLE: dev_table
  DB_CLUSTER_ARN: dev_cluster_arn
  DB_SECRET_ARN: dev_secret_arn

prod:
  <<: *default
  STAGE: prod_stage
  DEPLOY_BUCKET: prod_deploy_bucket
  ARTIFACT_BUCKET: prod_deploy_bucket
  TABLE: prod_table
  DB_CLUSTER_ARN: prod_cluster_arn
  DB_SECRET_ARN: prod_secret_arn
